h3 Profile

.row
  .col-xs-3
    form(role='form', novalidate, name='profileForm')
      .form-group
        label(for='username') Username
        input.form-control#username(type='username', name='username', ng-model='client.username', required)
      .form-group
        label(for='photo') Photo
        input.form-control#photo(type='photo', name='photo', ng-model='client.photo', required)
      button.btn.btn-primary(type='submit', ng-show='profileForm.$valid', ng-click='update()') Update Profile
  .col-xs-3
  .col-xs-3
    h3 {{client.username}}
    img.profilepic(ng-src='{{client.photo}}')
